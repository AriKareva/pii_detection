# Основные зависимости для извлечения текста и обработки файлов
beautifulsoup4>=4.12.0
pandas>=2.0.0
PyMuPDF>=1.23.0
striprtf>=0.0.26
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0
fitz

# OCR библиотеки (используем EasyOCR, он проще и стабильнее)
easyocr>=1.7.0
torch>=2.0.0
torchvision>=0.15.0

# Для работы с TIFF (LZW-сжатие и многостраничность)
tifffile>=2023.0.0
imagecodecs>=2023.0.0

# Для работы с Parquet
pyarrow>=12.0.0

# Для логирования и удобства (опционально)
tqdm>=4.65.0